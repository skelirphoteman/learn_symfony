{{ form_start(form_register) }}
	{{ form_errors(form_register) }}

	{{ form_row(form_register.name)}}
	{{ form_row(form_register.surname)}}
	{{ form_row(form_register.email)}}
	{{ form_row(form_register.password)}}
	{{ form_row(form_register.password)}}
	{{ form_row(form_register.save)}}
{{ form_start(form_register) }}